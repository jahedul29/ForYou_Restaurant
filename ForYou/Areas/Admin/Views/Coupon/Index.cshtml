@model IEnumerable<Coupon>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<br />
<div class="backgroundWhite border">
    <div class="row">
        <div class="col-6">
            <h3 class="text-info">Coupon List</h3>
        </div>
        <div class="col-6 text-right">
            <partial name="_CreateButtonPartial" />
        </div>
    </div>
    <br />
    <div>
        @if (Model.Count() > 0)
        {
            <table class="table table-striped">
                <tr class="table-secondary">
                    <th>@Html.DisplayNameFor(m => m.CouponName)</th>
                    <th>@Html.DisplayNameFor(m => m.Discount)</th>
                    <th>@Html.DisplayNameFor(m => m.MinimumAmount)</th>
                    <th>@Html.DisplayNameFor(m => m.IsActive)</th>
                    <th></th>
                    <th></th>
                </tr>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@Html.DisplayFor(m => item.CouponName)</td>
                        <td>@Html.DisplayFor(m => item.Discount)</td>
                        <td>@Html.DisplayFor(m => item.MinimumAmount)</td>
                        <td>@Html.DisplayFor(m => item.IsActive)</td>
                        <td><partial name="_TableButtonPartial" model="@item.CouponId" /> </td>
                    </tr>
                }

            </table>
        }
        else
        {
            <p> No Coupon exist...</p>
        }
    </div>
</div>

