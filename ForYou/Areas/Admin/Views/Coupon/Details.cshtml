@model Coupon
@{
    ViewData["Title"] = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<br />
<h3 class="text-info">Coupon Details</h3>
<br />

<div class="border backgroundWhite">
    <div class="row">
        <div class="col-8">
            <form method="post" enctype="multipart/form-data">
                <input type="hidden" asp-for="CouponId" />
                <div class="form-group row">
                    <div class="col-2">
                        <label asp-for="CouponName"></label>
                    </div>
                    <div class="col-5">
                        <input disabled asp-for="CouponName" class="form-control" />
                    </div>
                </div>
                <div class="form-group row">
                    <div class="col-2">
                        <label asp-for="Picture"></label>
                    </div>
                    <div class="col-5">
                        <input disabled type="file" id="projectImage" name="files" multiple class="form-control" />
                    </div>
                </div>
                <div class="form-group row">
                    <div class="col-2">
                        <label asp-for="CouponType"></label>
                    </div>
                    <div class="col-5">
                        <select disabled asp-for="CouponType" asp-items="Html.GetEnumSelectList<Coupon.ECouponType>()" class="form-control"></select>
                    </div>
                </div>

                <div class="form-group row">
                    <div class="col-2">
                        <label asp-for="Discount"></label>
                    </div>
                    <div class="col-5">
                        <input disabled asp-for="Discount" class="form-control" />
                    </div>
                </div>

                <div class="form-group row">
                    <div class="col-2">
                        <label asp-for="MinimumAmount"></label>
                    </div>
                    <div class="col-5">
                        <input disabled asp-for="MinimumAmount" class="form-control" />
                    </div>
                </div>

                <div class="form-group row">
                    <div class="col-2">
                        <label asp-for="IsActive"></label>
                    </div>
                    <div class="col-5">
                        <input disabled type="checkbox" asp-for="IsActive" class="form-control" />
                    </div>
                </div>
                <div class="form-group row">
                    <div class="col-5 offset-2">
                        <partial name="_EditAndBackToListButtonPartial" model="Model.CouponId" />
                    </div>
                </div>
            </form>

        </div>
        <div class="col-4">
            @{
                var base64 = Convert.ToBase64String(Model.Picture);
                var imgsrc = string.Format("data:image/jpg;base64,{0}", base64);
            }
            <img src="@imgsrc" height="100%" width="100%" />
        </div>
    </div>
</div>

@section Scripts{
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
